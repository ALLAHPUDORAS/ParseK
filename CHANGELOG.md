# Changelog

## Unreleased

- Apply validator and main updates: strict validation summary, contacts normalization
- Docker: use Playwright base image + named cache volume; Add rotating logs, validation summary, and graceful shutdown; Relax email filtering
